{
  "name": "identity-lib-project-template",
  "version": "0.0.1",
  "description": "Template project for library project of identity",
  "main": "index.js",
  "author": "Yoonyoul Yoo",
  "license": "MIT",
  "scripts": {
    "build": "webpack --env dev && webpack --env build && npm run test",
    "build:yarn": "webpack --env dev && webpack --env build && yarn run test",
    "dev": "webpack --progress --colors --watch --env dev",
    "test": "mocha --require babel-core/register --colors ./test/*.spec.js",
    "test:watch": "mocha --require babel-core/regiser --colors -w ./test/*.spec.js"
  },
  "dependencies": {},
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-core": "^6.26.0",
    "babel-eslint": "^8.2.3",
    "babel-loader": "^7.1.4",
    "babel-plugin-add-module-exports": "^0.2.1",
    "babel-preset-env": "^1.7.0",
    "chai": "^4.1.2",
    "eslint": "^4.19.1",
    "eslint-loader": "^2.0.0",
    "mocha": "^5.2.0",
    "uglifyjs-webpack-plugin": "^1.2.5",
    "webpack": "4.12.0",
    "webpack-cli": "^3.0.8",
    "yargs": "^11.0.0"
  }
}
